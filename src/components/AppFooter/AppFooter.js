import React from 'react';
import {Layout} from "antd";

const {Footer} = Layout;

function AppFooter(props) {
    return (
        <Footer style={{
            textAlign: 'center'
        }}>
            Test Management System 2023
        </Footer>

    );
}

export default AppFooter;